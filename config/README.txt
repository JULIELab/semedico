This directory is only required so ElasticSearch can follow that path 'config/../resources'
to find the resources configured in the index analyzer settings as given in
semedico-core/src/main/resources/semedicoElasticSearchSetup/indexAnalyzerSettings.yml